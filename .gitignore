__pycache__/
*.pyc
venv/
.venv/
.DS_Store
*.egg-info/

# environment variables / secrets — never commit these
.env
.env.local
*.env

# processed data is regenerated by main.py — no need to commit
data/processed/*.csv

# keep model artifacts committed (models/*.pkl, *.json) so the deployed
# Streamlit app works instantly without retraining